- hosts: localhost
  become: true
  tasks:
  - name: Install packages
    apt:
      name: ['htop', 'mc', 'tmux', 'neofetch']
  - name: Install the Flameshot package from flathub for current user
    community.general.flatpak:
      name: org.flameshot.Flameshot
      state: present
      method: user
